# SaveThePlate Backend - Local Development

# Database Connection
# Required: Get your Supabase PostgreSQL connection string from:
# 1. Go to your Supabase project dashboard
# 2. Settings → Database → Connection String → Pooler
# 3. Copy the connection string (format: postgresql://postgres.[PROJECT-REF]:[PASSWORD]@[HOST]:5432/postgres)
DATABASE_URL=

# Server Configuration
NODE_ENV="development"
PORT=3001

# Frontend URLs (leave as default for local development)
FRONT_URL=http://localhost:3000
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:3001

# CORS Settings (leave as default for local development)
CORS_ALLOWED_ORIGINS=http://localhost:3000
CORS_ALLOW_LOCALHOST=true

# Local directory for uploaded files (profile images, etc.)
STORAGE_PATH=./store
