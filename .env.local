# SaveThePlate Backend Local Development Environment
# This file contains test/development values and is committed to Git for new developers
# 
# ⚠️  IMPORTANT: Real secrets are managed via GitHub Secrets
# See: GITHUB_SECRETS_SETUP.md for production security configuration
#
# For local development, these test values are safe and ready to use

# ==================== DATABASE ====================
# LOCAL DEVELOPMENT - uses the PostgreSQL container from docker-compose.local.yml
DATABASE_URL="postgresql://savetheplate_user:local_dev_password@localhost:5432/savetheplate_local"
DB_NAME=savetheplate_local
DB_USER=savetheplate_user
DB_PASSWORD=local_dev_password

# ==================== JWT & SECURITY ====================
# Test key for local development (change in production via GitHub Secrets)
JWT_SECRET=dev-secret-key-change-in-production

# ==================== ENVIRONMENT ====================
NODE_ENV=development
PORT=3001

# ==================== REDIS ====================
# Uses Redis container from docker-compose.local.yml
REDIS_URL=redis://redis:6379

# ==================== FRONTEND URLs ====================
# Used for CORS and redirects
FRONT_URL=http://localhost:3000
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:3001

# ==================== CORS CONFIGURATION ====================
CORS_ALLOWED_ORIGINS=http://localhost:3000
CORS_ALLOWED_ORIGIN_SUFFIXES=
CORS_ALLOW_LOCALHOST=true

# ==================== EMAIL SERVICE ====================
# Test values for local development (real key in GitHub Secrets for production)
RESEND_TOKEN=test_token_local
RESEND_DOMAIN=savetheplate.local

# ==================== GOOGLE OAUTH ====================
# Test credentials for local development (real values in GitHub Secrets for production)
GOOGLE_CLIENT_ID=test_google_client_id
GOOGLE_CLIENT_SECRET=test_google_client_secret

# ==================== FACEBOOK OAUTH ====================
# Test credentials for local development (real values in GitHub Secrets for production)
FACEBOOK_APP_ID=test_facebook_app_id
FACEBOOK_APP_SECRET=test_facebook_app_secret

# ==================== GOOGLE MAPS ====================
# Test key for local development (real key in GitHub Secrets for production)
GOOGLE_MAPS_API_KEY=test_google_maps_key

# ==================== ADMIN CONFIGURATION ====================
# Local development admin emails
ADMIN_EMAILS=dev@savetheplate.local,admin@savetheplate.local

# ==================== API & SCRIPTS ====================
API_URL=http://localhost:3001
AUTH_TOKEN=test_auth_token_for_scripts

# ==================== SWAGGER DOCUMENTATION ====================
# Enabled for local development
ENABLE_SWAGGER=true
SWAGGER_USER=admin
SWAGGER_PASSWORD=change_this_password

# ==================== DEVELOPMENT SETTINGS ====================
ALLOW_UNSAFE_EVAL=false

# Storage path for uploads
STORAGE_PATH=./store
