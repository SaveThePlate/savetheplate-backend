import * as React from 'react';
declare function AnnouncementEmailTemplate({ title, description, details, buttonText, buttonLink, language, }: {
    title?: string;
    description?: string;
    details?: any[];
    buttonText?: string;
    buttonLink?: string;
    language?: string;
}): React.JSX.Element;
export default AnnouncementEmailTemplate;
