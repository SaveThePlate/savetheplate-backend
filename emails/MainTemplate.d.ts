import * as React from 'react';
declare function MainTemplate({ preview, mainTitle, description, centeredDescription, details, buttonText, buttonLink, withThank, withButton, language, }: {
    preview?: string;
    mainTitle?: string;
    description?: string;
    centeredDescription?: boolean;
    details?: any[];
    buttonText?: string;
    buttonLink?: string;
    withThank?: boolean;
    withButton?: boolean;
    language?: string;
}): React.JSX.Element;
export default MainTemplate;
